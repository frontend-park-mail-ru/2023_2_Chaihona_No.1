<div class="feed">
    <div class="feed__description">Лента</div>
    <div class="feed__empty" style="display: none;">
        <p class="feed__empty__noposts">Здесь пока что нет постов. Подпишитесь на авторов, чтобы отслеживать больше контента</p>
        <figure class="feed__empty__figure">
            <img class="feed__empty__search-img">
        </figure>
        {{!-- <input type="text" class="feed__empty__author-search" placeholder="Поиск автора...">
        <div class="feed__empty__searched" style="display: none">
            <div class="feed__empty__searched__count"></div>
            <div class="feed__empty__searched__profiles"></div>
        </div> --}}
        {{!-- <button class="feed__empty__search-btn">Искать авторов</button> --}}
    </div>
    {{#each posts}}
        {{>post post=this isOwner=false}}
    {{/each}}
</div>
